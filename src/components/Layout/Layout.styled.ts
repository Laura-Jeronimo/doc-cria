import { styled, Box } from "@mui/material";

export const StyledLayoutContainer = styled(Box)(({ theme }) => ({
    height: "100%",
    width: "100%",
    margin: 0,
    padding: 0,
    overflowY: "auto",
    background:  "#ffffff",
}));